/**
 * Phase 3 stub: Figma write-back (value + description only, allow-listed).
 * Not wired until bidirectional sync — exits with instructions.
 */
console.log(`
figma-writeback.ts — Phase 3

Will PATCH only value/description on variables listed in
tokens/_registry/figma-mapping.json after a merged GitHub change.

Not implemented in Phase 1. Complete Phases 1–2 first.
`);
process.exit(0);
